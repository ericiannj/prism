# v0.6 — Polish

**Goal:** Production-ready, fully tested, portfolio-presentable.

## Tasks

- [ ] Error boundaries in `apps/web`: catch render errors, show fallback UI
- [ ] Toast notifications for all async operations (upload success/error, send message error)
- [ ] Loading states across all async flows (upload spinner, chat send button, skeleton loaders)
- [ ] Delete document with cascade chunk deletion (`DELETE /documents/:id`)
- [ ] Rename chat session (inline edit on session list)
- [ ] React Testing Library component tests for web (key flows: upload, chat, login)
- [ ] Vitest coverage report: `pnpm test --coverage`
- [ ] README: project overview, setup instructions, screenshots
- [ ] `docker-compose.prod.yml`: production-like setup with proper env handling
- [ ] Final review: remove all `DEV_USER_ID` references, console.logs, TODOs

## Deliverables

- No unhandled errors — every failure shows a meaningful message
- Every async action has a visible loading state
- Documents can be deleted; chat sessions can be renamed
- Coverage report generated
- README is clear enough for a recruiter to run the project locally

## Manual Testing

```bash
# 1. Full happy path (regression check)
docker compose up -d && pnpm dev

# Register → upload a doc → wait for ready → open chat
# Ask about the doc → embeddings badge → expand badge → see chunks
# Ask a web question → web badge
# Ask a mixed question → mixed badge

# 2. Error states
# Upload a corrupted or unsupported file → toast error appears
# Disconnect from internet → send a chat message → graceful error

# 3. Delete a document
open http://localhost:5173/documents
# Click delete on a document → confirm → document disappears
psql $DATABASE_URL -c "SELECT COUNT(*) FROM document_chunks WHERE document_id='<deleted-id>';"
# Expected: 0 rows (cascade delete worked)

# 4. Rename a chat session
open http://localhost:5173/chat
# Click on session title → edit → save → title updates

# 5. Coverage
pnpm test --coverage
# Check report for critical paths: ingestion, tool loop, auth middleware

# 6. README test
# Follow README from scratch in a new terminal session
# Should work with only: git clone + docker compose up -d + pnpm install + pnpm dev
```

## Done When

- Full happy path works without any console errors
- All error states show user-friendly messages (no raw stack traces)
- `pnpm test --coverage` shows reasonable coverage on critical paths
- README setup instructions work from scratch
- Project is presentable as a portfolio piece
